# Gerado automaticamente por `openspace init` — este arquivo é só um exemplo.
# Nunca commite o .env real (já está no .gitignore).

POSTGRES_USER=openspace
POSTGRES_PASSWORD=changeme
POSTGRES_DB=openspace
DATABASE_URL=postgresql://openspace:changeme@postgres:5432/openspace?schema=openspace

# Gerado com 48 bytes aleatórios por `openspace init` — nunca reaproveite este valor.
JWT_SIGNING_SECRET=changeme-generate-with-openspace-init

CORS_ORIGIN=http://localhost:8080
ENVIRONMENT_TIER=development

API_PORT=4000
DASHBOARD_PORT=8080
